{
  "name": "pablo-web",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler pages deploy dist --project-name pablo-web"
  },
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "react-router-dom": "^7.9.2"
  },
  "devDependencies": {
    "@types/react": "19.0.10",
    "@types/react-dom": "19.0.4",
    "@vitejs/plugin-react": "5.0.0",
    "@cloudflare/workers-types": "^4.20250210.0",
    "babel-plugin-react-compiler": "^1.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.17.0"
  }
}
